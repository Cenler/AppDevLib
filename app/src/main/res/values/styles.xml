<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- 全局主题配置 -->
        <item name="android:screenOrientation">portrait</item><!-- 横竖屏锁定 -->
        <item name="colorPrimaryDark">@color/color_ink</item><!-- Statebar Color 5.0支持 -->
        <item name="colorPrimary">@color/color_ink</item><!-- Toolbar Color -->
        <item name="colorAccent">@color/color_blue_assist</item><!-- Metral Design -->
        <item name="android:textColorPrimary">#FFCFCFCF</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="AppTheme.Splash">
        <item name="android:windowBackground">@mipmap/img_welcome_splash</item><!-- 冷启动优化 -->
    </style>

    <style name="AppTheme.SwipeBack">
        <item name="android:windowIsTranslucent">true</item><!-- SwipeBack 黑屏处理 -->
    </style>

    <style name="CollapsingToolbarTitleStyle" parent="@android:style/TextAppearance">
        <item name="android:textColor">@android:color/transparent</item>
    </style>

    <!-- BaseDialog 基本样式 -->
    <style name="BaseDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
    </style>

</resources>
